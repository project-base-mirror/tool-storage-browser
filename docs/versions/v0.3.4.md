# S3 Explorer v0.3.4

- 日期：2026-07-25
- 状态：由 Git 历史回填；当时未创建 tag/Release
- 主题：文件夹传输批次与失败恢复

## 已交付

- 文件夹上传/下载创建批次，聚合总文件、成功、失败、跳过和字节进度。
- 单文件失败不会中断整个文件夹批次。
- 支持暂停/继续/取消整个批次和失败项重试。
- 传输面板增加失败详情与针对失败项的操作入口。
- 改进批次完成状态与任务生命周期一致性。

## 行为与兼容性

任务仍逐文件持久化，批次提供聚合视图。重试只重新排队可重试失败项，不复制已完成结果。

## 验证

- 批次状态、聚合计数、取消、失败隔离和重试测试。
- UI 失败详情运行检查。

## 已知限制

此版本还不是 Folder Sync；它只提供可靠的文件夹传输基础。

## 关联提交

- `bb67fb9` `feat: add resilient folder transfer batches`
- `ddc4e74` `feat(ui): add failure details and retry actions`
