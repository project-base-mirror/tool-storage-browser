# Next / Unreleased

本文件只记录已经合入 `main`、但尚未进入正式版本 tag 的变化。它不是版本号，也不表示已经正式发布。最近正式版本仍是 `v0.8.1`；已发布历史见 [`README.md`](README.md)。

## 新功能

- 新增顶级“凭据”菜单和独立凭据中心；CDN 配置中心只保留 CDN 配置与 Bucket/前缀关联。
- 新增权限检查列表，按凭据与目标保存最近一次脱敏结果，并在桌面端提供需要逐次确认的对象存储写入/删除/ACL 探针。

## 修复

- 暂无。

## 维护

- Debug 构建与附加调试器启动使用独立实例锁、`%APPDATA%\S3Explorer.Debug` 数据目录和 `[Debug]` 窗口标记，可在正式安装版驻留托盘时直接从 Visual Studio 验证界面，且不会读写正式配置。

## 兼容性 / 数据格式

- 暂无。

## 已知限制

- 暂无。

## 发布时处理

1. 选择下一个版本号后，把本文件中属于该版本的条目整理到新的 `vX.Y.Z.md`。
2. 完成版本号、更新清单、Pages 和发布包文件名的一致性检查。
3. 在同一个发布提交中把本文件重置为本模板，并保留空的“修复 / 维护 / 兼容性 / 已知限制”章节。
