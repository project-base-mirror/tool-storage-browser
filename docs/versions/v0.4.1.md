# S3 Explorer v0.4.1

- 日期：2026-07-25–26
- 状态：由 Git 历史回填；当时未创建 tag/Release
- 主题：Bucket 管理、导航与 UI 自动化

## 已交付

- Bucket 属性总览与 Provider capability 提示。
- Bucket ACL、Bucket Policy、Public Access Block、Object Ownership 读取和编辑。
- 安全清空 Bucket：统计普通对象、版本、Delete Marker 和未完成 Multipart，再确认执行。
- 修复 MinIO 创建 Bucket 时请求路由到 API root。
- 发布打包每次从干净输出重新生成，避免旧文件混入。
- 对象列表增加 `..` 上级目录行。
- 增加隔离数据目录的 WinForms 自动化协议、Smoke 脚本、版本/状态命令和 App.Tests。

## 行为与兼容性

管理页按能力显示支持状态；不支持的 API 不伪装为成功。清空启用版本的 Bucket 会显式处理所有版本和 Delete Marker。

## 验证

- Policy/ACL 规范化、能力映射和危险确认测试。
- MinIO Bucket 管理集成测试。
- UI Smoke 验证窗口、菜单、工具栏、导航、对象列表、队列和状态栏。
- 发布包干净重建检查。

## 已知限制

CORS、生命周期、版本管理和 Object Lock 仍未实现；这些入口保持禁用。

## 关联提交

- `e252c12` `feat: add bucket properties policy acl and safe empty for v0.4.1`
- `8652776` `fix(minio): route bucket creation to API root`
- `e8b44f6` `fix(packaging): rebuild release artifacts cleanly`
- `34e5016` `feat(ui): add parent directory navigation row`
- `921a5ae` `feat(automation): add application CLI and UI smoke checks`
