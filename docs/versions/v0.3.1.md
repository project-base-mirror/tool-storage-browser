# S3 Explorer v0.3.1

- 日期：2026-07-24
- 状态：由 Git 历史回填；当时未创建 tag/Release
- 主题：持久化传输队列

## 已交付

- 传输任务写入 JSON Store，异常退出后可以恢复未完成任务。
- PersistentTransferQueue 负责排队、并发、暂停、继续、取消、重试和状态通知。
- WinForms 传输面板接入持久队列，应用关闭时区分等待、取消和返回。
- 引入 S3TransferTaskExecutor，隔离队列编排与具体存储操作。

## 行为与兼容性

升级后新任务进入持久队列；旧版本没有可迁移的持久队列文件。损坏的任务数据不会被静默执行。

## 验证

- 队列调度、并发、恢复、取消、重试和保存失败测试。
- Release 构建和窗口关闭流程检查。

## 已知限制

下载与 Multipart 的字节级检查点尚未完成，恢复可能从任务级重新开始。

## 关联提交

- `5c5276f` `feat: add recoverable transfer queue for v0.3.1`
