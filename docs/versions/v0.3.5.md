# S3 Explorer v0.3.5

- 日期：2026-07-25
- 状态：由 Git 历史回填；当时未创建 tag/Release
- 主题：MinIO Endpoint 安全校验

## 已交付

- 识别常见 MinIO Console 端口/地址，阻止把 Console Endpoint 当作 S3 API Endpoint 创建 Bucket。
- 连接校验给出面向用户的修复说明。
- Client Factory 与兼容策略增加对应防回归测试。
- 错误对话框补充 Endpoint 上下文。

## 行为与兼容性

以前可能一直返回重定向或 HTML 的 Console 地址会在请求前被拒绝。正确的 MinIO API Endpoint 不受影响。

## 验证

- Endpoint 分类、已知端口与自定义端口测试。
- Release 构建通过。

## 已知限制

自定义反向代理可能无法仅按端口判断，仍需连接测试确认实际 API。

## 关联提交

- `b34d95d` `fix: reject MinIO console endpoints for bucket creation`
