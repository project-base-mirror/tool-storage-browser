# S3 Explorer v0.3.2

- 日期：2026-07-24
- 状态：由 Git 历史回填；当时未创建 tag/Release
- 主题：断点续传、限速与电源管理

## 已交付

- 下载使用临时文件和原子替换，避免失败时留下看似完整的目标文件。
- 检查远端 ETag/VersionId，源对象变化时拒绝错误续传。
- 上传与下载支持带宽限制、指数退避和运行时并发配置。
- 活跃传输期间使用 Windows 电源请求防止系统睡眠，空闲后释放。
- 设置页增加重试、并发、Multipart 阈值/分片和上下行限速。

## 行为与兼容性

限速为 0 表示不限速。临时文件仅在成功完成后替换目标；不兼容的历史检查点会安全重启任务。

## 验证

- 原子落盘、远端身份、续传范围、限速和重试计算测试。
- Provider 集成测试同步调整。

## 已知限制

Multipart 上传的服务端分片对账和残留清理在 v0.3.3 完成。

## 关联提交

- `9d5d622` `feat: add resumable governed transfers for v0.3.2`
